import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'employee-social-media-app',
  webDir: 'dist'
};

export default config;
